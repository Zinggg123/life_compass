<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zing.compass.mapper.CouponMapper">
    <select id="selectCouponById" resultType="com.zing.compass.entity.Coupon">
        SELECT * FROM coupon
        WHERE coupon_id = #{couponId}
    </select>

    <update id="updateCouponStatus">
        UPDATE coupon
        SET status = #{status}
        WHERE coupon_id = #{couponId}
    </update>

    <select id="selectCouponsByBizId" resultType="com.zing.compass.entity.Coupon">
        SELECT * FROM coupon
        WHERE biz_id = #{bizId}
    </select>

    <update id="updateStockDecrease">
        UPDATE coupon
        SET available_quantity = available_quantity - 1
        WHERE coupon_id = #{couponId} AND available_quantity > 0
    </update>

    <insert id="insertCoupon">
        INSERT INTO coupon (coupon_id, biz_id, name, description, discount_amount, threshold_amount, valid_from, valid_to, total_quantity, available_quantity, status, create_time)
        VALUES (#{couponId}, #{bizId}, #{name}, #{description}, #{discountAmount}, #{thresholdAmount}, #{validFrom}, #{validTo}, #{totalQuantity}, #{availableQuantity}, #{status}, #{createTime})
    </insert>

    <select id="selectFutureValidCoupons" resultType="com.zing.compass.entity.Coupon">
        SELECT * FROM coupon
        WHERE status = 1
        AND valid_from > #{now}
        AND valid_from &lt;= #{threshold} <!-- 开始时间 <= 阈值时间 -->
    </select>

    <select id="selectExpiredCouponIds" resultType="java.lang.String">
        SELECT coupon_id FROM coupon
        WHERE valid_to &lt; #{now}
    </select>
</mapper>